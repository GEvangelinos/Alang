// __CMP_RUN__: %DRIVER --source %SELF %ERROR_MODE --export_ir --export_symbol_table_without_temps --export_diagnostics

// __BEGIN_SOURCE__
x;

{
    // Error: can't create local symbol with libfunc name
    local print;

    function foo(){
        // Legal: new variable name, no conflict as no function with same name exists in scope 2
        local foo;

    }

    // Legal: finds function foo above
    local foo;

}

// Legal: libfuncs are declared on scope 0, so, finds 'print'
local print;
// __END_SOURCE__

// __BEGIN_DIAGNOSTICS__
// code,line,column,issue_type
// LOCAL_ID_SHADOWS_LIBFUNC,5,5,error
// __END_DIAGNOSTICS__
